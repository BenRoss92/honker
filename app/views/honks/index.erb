<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Honks Feed</title>
  </head>
  <body>
    <% if @current_user %>
    <h2>Welcome <%= @current_user.name %>!</h2>
    <% else %>
    <h2>Welcome to Honk!</h2>
    <% end %>
    <a href="/users/sign_up">Sign Up</a>
    <h1>Live Honks</h1>
    <ul id="honks">
      <% @honks.each do |honk| %>
      <li>
        <p>@<%= honk.user.username %>: <%= honk.message.upcase %><br>
          Posted at: <%= honk.posted_at %></p>
      </li>
      <% end %>
    </ul>
  </body>
</html>
